<%-include("./partials/header") -%>
<h1>Home</h1>
<% blogs.forEach(blog=>{ %>
<h1><%=blog.title %></h1>   
<p> <%=blog.content.substring(0, 100)%> <a href="/blogs/posts/<%= blog._id%>"> Read More</a></p>

<p>Make a comment</p>
<!--
<form
        class="input-group mb-3"
        method="POST"
        action="posts/makecomment/<%=blog._id %>"
      >
        <input
          type="text"
          id="text"
          name="comment"
          class="form-control"
          placeholder="Comment something!"
        />
        <button type="submit" class="btn btn-primary">Comment</button>
      </form>
    </div>
	

<%})%>
  -->
<%-include("./partials/footer") -%>

